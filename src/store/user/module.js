import * as type from './type'

const mutations = {
  [type.FETCH_ABOUTME] (state, payload) {}
}

const actions = {
  [type.FETCH_ABOUTME] (context, payload) {}
}

export default {
  state: {
    aboutme: {}
  },
  mutations,
  actions
}
