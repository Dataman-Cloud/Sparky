export const listFilter = (list, name, keyword) => {
  // console.log('its ok')
  // if (arguments.length === 0) return
  // if (!keyword || typeof keyword !== 'string') return list
  // if (Array.isArray(list) && list.length > 0) {
  //   let kw = new RegExp(keyword)
  //   return list.filter((item) => kw.test(item[name]))
  // }
}
